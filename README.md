# TikTok Parser - Парсер роликов по теме "тушь для ресниц"

Парсер для сбора популярных TikTok-роликов за последние 7 дней по заданной теме с фильтрацией дублей и сортировкой по популярности.

## 📋 Описание

Скрипт собирает данные о TikTok-роликах по теме "тушь для ресниц" используя следующие поисковые запросы:
- Ключевые слова: "тушь для ресниц", "стойкая тушь", "объемная тушь"
- Хештеги: #тушь, #ресницы, #макияж

## 🚀 Установка и запуск

### Требования
- Python 3.8 или выше
- pip (менеджер пакетов Python)

### Шаг 1: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 2: Установка Playwright браузеров

TikTokApi использует Playwright для обхода защиты TikTok. Необходимо установить браузеры:

```bash
playwright install chromium
```

**Важно:** На Windows может потребоваться запуск PowerShell от имени администратора.

### Шаг 3: Запуск парсера

```bash
python parser.py
```

Парсер автоматически:
1. Инициализирует подключение к TikTok API
2. Выполнит поиск по всем запросам
3. Отфильтрует ролики за последние 7 дней
4. Удалит дубликаты
5. Отсортирует по популярности (просмотры)
6. Сохранит результат в файл `результат.csv`

## 📊 Формат выходных данных

Результаты сохраняются в CSV файл `результат.csv` со следующими колонками:

- **url** - ссылка на ролик
- **description** - описание ролика
- **views** - количество просмотров
- **likes** - количество лайков
- **date** - дата публикации
- **hashtags** - список хештегов
- **author** - имя автора

## ⚙️ Настройка

Вы можете изменить параметры парсера в файле `parser.py`:

- **Количество результатов на запрос**: измените `max_results` в методе `search_videos()`
- **Поисковые запросы**: измените списки `keywords` и `hashtags` в методе `get_search_queries()`
- **Задержки между запросами**: настройте параметры в методе `random_delay()`

## 🔧 Решение проблем

### Ошибка "playwright not found"
```bash
playwright install chromium
```

### Ошибка "TikTokApi initialization failed"
- Убедитесь, что установлены все зависимости: `pip install -r requirements.txt`
- Проверьте подключение к интернету
- Попробуйте запустить снова через несколько минут (возможны rate limits)

### Мало результатов (< 20 видео)
- Увеличьте `max_results` в коде
- Добавьте больше поисковых запросов
- Запустите парсер в разное время суток

### Ошибки кодировки в CSV
Файл сохраняется в кодировке UTF-8 с BOM (`utf-8-sig`), что обеспечивает корректное отображение в Excel.

## 📝 Структура проекта

```
.
├── parser.py          # Основной скрипт парсера
├── requirements.txt   # Зависимости Python
├── README.md         # Документация (этот файл)
└── результат.csv     # Результаты парсинга (создается после запуска)
```

## 🛠️ Технические детали

### Используемые библиотеки
- **TikTokApi** - для работы с TikTok API
- **Playwright** - для обхода антибота TikTok
- **asyncio** - для асинхронных запросов
- **csv** - для сохранения результатов

### Алгоритм работы
1. Формирование списка поисковых запросов
2. Для каждого запроса получение списка роликов через TikTokApi
3. Фильтрация роликов по дате (последние 7 дней)
4. Удаление дублей по ID
5. Сбор требуемых полей (ссылка, описание, просмотры, лайки, дата, хештеги)
6. Сортировка по популярности (просмотры)
7. Сохранение в CSV

## ⚠️ Сложности и ограничения

### Антибот-защита TikTok

TikTok активно использует сложные механизмы защиты от автоматизации, включая:
- **Генерацию подписей запросов** - каждый запрос требует специальной криптографической подписи
- **Анализ поведения браузера** - отслеживание движений мыши, нажатий клавиш, временных паттернов
- **Проверку User-Agent и заголовков** - валидация реалистичности браузерных заголовков
- **Капчи и верификацию** - при подозрительной активности могут появляться капчи

**Решение:** Использование TikTokApi с Playwright позволяет автоматически генерировать необходимые подписи и эмулировать реальное поведение браузера. Однако это не гарантирует 100% успех, так как TikTok постоянно обновляет свои механизмы защиты.

### Rate Limits и ограничения API

TikTok устанавливает строгие лимиты на количество запросов:
- **Временные ограничения** - ограничение количества запросов в единицу времени
- **IP-блокировки** - при превышении лимитов IP может быть временно заблокирован
- **Нестабильность API** - TikTok может изменять структуру API без предупреждения

**Решение:** Реализованы случайные задержки между запросами (2-4 секунды) и обработка ошибок с повторными попытками. При возникновении блокировок рекомендуется подождать 15-30 минут перед повторным запуском.

### Фильтрация по времени

Фильтрация роликов за последние 7 дней может быть затруднена по нескольким причинам:
- **Неточность временных меток** - TikTok может предоставлять время в разных форматах или часовых поясах
- **Ограниченная доступность данных** - не все ролики имеют точную информацию о дате публикации
- **Кэширование результатов** - API может возвращать кэшированные данные, не отражающие актуальное состояние

**Решение:** Реализована проверка временных меток с учетом возможных форматов и обработка случаев отсутствия данных о дате.

### Нестабильность API и изменения структуры

TikTok регулярно обновляет свою платформу и API:
- **Изменение структуры ответов** - формат данных может изменяться без предупреждения
- **Устаревание методов** - старые методы API могут перестать работать
- **Отсутствие официальной документации** - TikTok не предоставляет публичную документацию API

**Решение:** Код включает обработку исключений и проверку наличия полей перед их использованием. При изменении структуры API может потребоваться обновление кода парсера.

## 📄 Лицензия

Этот проект создан в образовательных целях. Использование парсеров может нарушать условия использования TikTok. Используйте на свой риск.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что Playwright браузеры установлены
3. Проверьте подключение к интернету
4. Попробуйте запустить снова через некоторое время

---

**Примечание:** TikTok активно борется с автоматизацией, поэтому парсер может требовать периодических обновлений для поддержания работоспособности.

